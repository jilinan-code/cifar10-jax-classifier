# CIFAR-10 Image Classification with JAX and Flax

A convolutional neural network implemented in JAX/Flax for classifying CIFAR-10 images. This project demonstrates how to build, train, and evaluate a CNN using modern JAX-based machine learning frameworks.

## 🚀 Features

- **Framework**: JAX + Flax for neural network implementation
- **Model**: Convolutional Neural Network (CNN)
- **Dataset**: CIFAR-10 (10 classes)
- **Training**: Adam optimizer with cross-entropy loss
- **GPU/CPU Support**: Configurable device settings
